import React from 'react';
import { Button, Container } from '@mui/material';


const Home = () => {
    return (
        <Container sx={ { bgcolor:"primary.main", display:"flex", flexDirection:"column" } } maxWidth="md">
            메인
        </Container>
    );
};

export default Home;